name: Master
on:
  push:
    branches:
      - 'master'
      
jobs:
  test-setup-ruby:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true