<section class='main-content'>

  <div class='form-titulo'>Editar renda</div>

  <div class='white-box'>

    <% if @renda.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@renda.errors.count, "erro") %> impedem que a renda seja gravada no banco de dados:</h2>
    
        <ul>
        <% @renda.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <%= form_for(@renda) do |form| %>
      <p class='form-label'><%= form.label :profissão %></p>
      <p class='form-input'><%= form.text_field :profissao %></p>
    
      <p class='form-label'><%= form.label :empresa %></p>
      <p class='form-input'><%= form.text_field :empresa %></p>

      <p class='form-label'><%= form.label :salário %></p>
      <p class='form-input'><%= form.number_field :salario, step: :any %></p>

      <p class='form-btn'><%= form.submit 'Atualizar renda'%></p>
    <% end %>
  </div>

</section>